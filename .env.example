# The external data root is intentionally outside the Obsidian vault.
DATACODING_DATA_DIR=~/Desktop/geek/DataCoding-data

# Optional override. Defaults to $DATACODING_DATA_DIR/outputs.
# DATACODING_ARTIFACTS_DIR=/absolute/path/to/generated-artifacts
